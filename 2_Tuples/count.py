# Count is used to count the element in the letter

my_tuple = ["a", "p", "p", "l", "e"]

print(my_tuple.count("p")) 
print(my_tuple.count("l"))

# If the elements are not present in the tuple then it will return 0
print(my_tuple.count("h"))